<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container-fluid">
    <a href="/" class="navbar-brand">yDown</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent"
      aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <div class="navbar-nav me-auto mb-2 mb-lg-0">
        {{#if session.isLoggedIn}}
        <a class="nav-link {{#ifEquals title "Library"}}active{{/ifEquals}}" href="/videos">Library</a>
        <a class="nav-link" href="/logout">Logout</a>
        {{else}}
        <a class="nav-link {{#ifEquals title "Login"}}active{{/ifEquals}}" href="/login">Login</a>
        <a class="nav-link {{#ifEquals title "Signup"}}active{{/ifEquals}}" href="/signup">Signup</a>
        {{/if}}
      </div>
        {{#if session.isLoggedIn}}
      <form class="d-flex" action="/videos" method="POST">
        <input class="form-control me-2" type="url" name="url" placeholder="Video URL">
        <button class="btn btn-outline-success" type="submit">Add</button>
      </form>
        {{/if}}
    </div>
  </div>
</nav>